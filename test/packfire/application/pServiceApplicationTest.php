<?php

pload('packfire.application.pServiceApplication');

/**
 * Test class for pServiceApplication.
 * Generated by PHPUnit on 2012-09-17 at 06:20:35.
 */
class pServiceApplicationTest extends PHPUnit_Framework_TestCase {

    /**
     * @var pServiceApplication
     */
    protected $object;

    /**
     * Sets up the fixture, for example, opens a network connection.
     * This method is called before a test is executed.
     */
    protected function setUp() {
        $this->object = $this->getMockForAbstractClass('pServiceApplication');
    }

    /**
     * Tears down the fixture, for example, closes a network connection.
     * This method is called after a test is executed.
     */
    protected function tearDown() {
        
    }
    
    public function testConstruct(){
        $this->assertInstanceOf('pBucketUser', $this->object);
        $property = new \ReflectionProperty(get_class($this->object), 'services');
        $property->setAccessible(true);
        $services = $property->getValue($this->object);
        $this->assertTrue($services->contains('config.app'));
        $this->assertFalse($services->contains('config.routing'));
        $this->assertNull($this->object->service('config.routing'));
    }

}
